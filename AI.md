# AI 药物发现平台 - 模型中心与任务中心需求文档

## 1. 概述
本文档详述了 AI 药物发现平台中“模型中心”与“任务中心”两大核心模块的功能需求、交互逻辑及页面设计规范。平台通过集成多种深度学习模型，支持从靶标发现、分子生成到合成路线规划的全流程 AI 辅助药物开发。

---

## 2. 模型中心 (Model Center)
模型中心是用户选择并配置 AI 推理任务的入口。

### 核心功能
*   **模型展示**：以卡片形式展示所有可用的 AI 模型。
*   **搜索与分类**：支持按关键词搜索模型名称/描述，并按类别（蛋白、分子、靶标等）进行过滤。
*   **任务跳转**：点击模型卡片进入该模型的任务配置（推理）页面或查看历史记录。

### 模型定义详解

#### 2.1 津智-蛋白质结构预测模型 (m1)
*   **功能描述**：基于改良 AlphaFold2 架构，提供亚原子级精度的蛋白质三维结构预测，支持多链复合物建模。
*   **核心特性**：
    1.  支持创建任务时自定义任务名称。
    2.  支持直接输入氨基酸序列或上传单个像素不大于 15MB 的 fasta/txt 格式文件。
    3.  提供蛋白质结构结果的可视化展示及下载功能。
*   **输入项**：
    *   **任务基本信息**：任务名称（必填）、任务描述（可选）。
    *   **序列录入**：支持手动输入 FASTA 格式序列或上传 `.fasta`/`.fa`/`.txt` 文件。
    *   **生成数量**：生成蛋白质结构数量，范围为 1~5。
*   **校验规则**：
    *   **文本输入校验**：
        1.  去除空格、换行、Tab，并转成大写。
        2.  不能为空，只能包含 ARNDCQEGHILKMFPSTWYVBZXUO。
        3.  不允许出现 `* -` 数字、中文、特殊符号。
        4.  长度范围 20–4000 aa。
        5.  X 占比不超过 10%。
        6.  检测疑似核酸序列提示（若仅由 A/T/C/G/U/N 组成）。
    *   **文件上传校验**：
        1.  格式支持 `.fasta`, `.fa`, `.txt`。
        2.  大小 ≤ 15MB，且只能包含一条序列。
    *   **实时反馈**：展示当前序列长度、字符检查状态、序列类型及风险提示（如：含 X 个未知氨基酸）。
*   **输出项**：
    *   PDB 结构文件、蛋白质结构 3D 渲染。
*   **事件流**：
    1.  用户在模型中心点击“蛋白质结构预测”卡片。
    2.  录入序列后，实时展示校验信息。
    3.  提交任务后跳转至任务中心。
    4.  任务完成后，点击“查看详情”进行 3D 可视化。

#### 2.2 基于结构的虚拟筛选 (m2)
*   **功能描述**：基于 AutoDock Vina 的分子对接算法，快速从大规模小分子库中筛选潜在活性化合物。
*   **支持格式**：受体与配体文件支持 `pdbqt`, `pdb`, `ent`, `xyz`, `pqr`, `mcif`, `mmcif` 等。
*   **输入项**：
    *   **任务基本信息**：任务名称、任务描述。
    *   **受体输入**：上传受体文件，支持是否预处理为 PDBQT 的开关。
    *   **配体输入**：上传自定义小分子集（SDF/CSV），支持格式：`.pdbqt`, `.sdf`, `.mol2`, `.mol`, `.csv`。
    *   **参数配置**：
        *   **对接位点**：定义中心坐标 (X, Y, Z) 及搜索范围大小 (X, Y, Z)。
        *   **最大能量差**：指定与最优模型相差的最大能量值。
        *   **计算资源**：指定 CPU 数量。
        *   **对接精度**：指定对接细致程度 (Exhaustiveness)。
*   **输出项**：
    *   **对接得分排名列表**：包含分子 ID、分子名称、Docking Score、Rank、Pose 数量。
    *   支持导出虚拟筛选结果及下载小分子构象文件。
*   **推荐方案**：AutoDock Vina, Meeko, RDKit, NGL Viewer。

#### 2.3 靶标发现与验证模型 (m9)
*   **功能描述**：结合多组学数据与 NLP 技术，从海量文献、组学数据中挖掘疾病相关的新型潜在靶标。
*   **输入项**：
    *   **任务基本信息**：任务名称、任务描述。
    *   **核心参数**：
        *   **疾病名称**：关键词输入。
        *   **组学数据**：上传差异表达基因列表（CSV/TSV，可选）。
        *   **分析权重**：文献支持、遗传学证据、PPI 网络权重调节、TopK 候选靶标数。
*   **输出项**：
    *   **候选靶标热度表**：包含靶标名称 (Symbol)、综合评分、文献评分、组学评分、网络评分、遗传学评分等。
    *   **可视化**：基因-疾病关联网络图。
*   **推荐模型**：PubMedBERT, SciBERT, NetworkX, PyG。

#### 2.4 潜在靶标预测 (m10)
*   **功能描述**：通过图深度学习算法高效预测分子的潜在作用靶标，识别脱靶风险。
*   **输入项**：
    *   **查询分子**：输入 SMILES 字符串或上传 `.sdf`, `.mol`, `.csv`。
    *   **靶标库**：选择系统内置靶标库。
    *   **置信度阈值**：设定过滤分数。
*   **输出项**：
    *   **预测靶标排名表**：包含 Target ID, Name, Class, Affinity Score, Rank, Evidence Note。
    *   **操作**：支持排名表导出为 CSV 或 Excel。

#### 2.5 骨架跃迁分子生成 (m11)
*   **功能描述**：在保持先导化合物活性的基础上，通过替换核心骨架产生新颖的化合物。
*   **输入项**：
    *   **参考分子**：SMILES 输入或上传 `.sdf`, `.mol`。
    *   **任务模式**：自动骨架跃迁 / 指定片段替换。
    *   **定制参数**：待替换片段 (SMARTS)、替换策略（骨架库/BRICS）。
    *   **约束条件**：生成数量、最大返回数、PAINS 过滤。
*   **输出项**：
    *   **候选分子列表**：含排名、2D 结构、Scaffold Similarity、Novelty Score、合成可行性、替换区域说明。
    *   支持导出生成结果。

#### 2.6 强化学习小分子生成模型 (m12)
*   **功能描述**：利用强化学习算法，面向特定靶点定制生成满足多种属性（活性、QED、LogP）的分子。
*   **输入项**：
    *   **靶点信息**：靶点名称/编号。
    *   **生成模式**：从头生成 / 参考优化。
    *   **奖励函数调节**：通过滑块调节 Activity, QED, LogP, SAS, Diversity 的权重。
*   **输出项**：
    *   **生成分子列表**：按综合评分排序，展示各子项评分（SMY, QED, LogP等）。
    *   支持导出为 CSV。
*   **推荐方案**：REINVENT。

#### 2.7 合成路线规划 (m13)
*   **功能描述**：生成目标分子的逆合成路线，并评估前体清单、收率及合成成本。
*   **输入项**：
    *   **目标分子**：SMILES 输入或上传文件。
    *   **规划参数**：最大路线数、最大步骤数、原料库来源、轻量相互作用分析开关。
*   **输出项**：
    *   **推荐路线列表**：包含路线 ID、评分、步数、前体清单、预估成本、预估收率。
    *   **可视化**：树状逆合成路线图，支持下载为 SVG。

---

## 3. 任务中心 (Task Center)

任务中心是全平台推理任务的中央控制台。

### 核心功能
*   **状态监控**：实时查看任务运行进度（百分比）和状态（队列中、执行中、成功、失败）。
*   **任务管理**：
    *   **查询**：按任务 ID、名称、类型进行多维搜索。
    *   **操作**：刷新状态、查看详情、删除任务、新建任务。
*   **列表字段**：
    *   任务编号 (ID)
    *   任务名称 (Name)
    *   任务类型 (Type)
    *   使用模型 (Model)
    *   当前进度 (Progress Bar)
    *   状态标签 (Status Badge)
    *   创建/运行时间
    *   操作列

### 页面设计原则
*   **技术感**：采用单色系设计，强调数据密度与系统感（Tech-Mono 字体，网格线背景）。
*   **即时反馈**：状态流转通过动画与色彩提示。

---

*文档版本：v1.0*
*更新日期：2026-04-21*
